# 2026-08-14 — Ming Workbench Desktop autonomous dev log

## P0-A milestone (authoritative / evidence / resume execution chain) — DONE
- Fixed the one failing test in `test/repository-evidence.test.mjs`:
  - `reconcileExternalEffect` was defined (as async) in `bounded-execution.ts` but the
    test imported it synchronously from `repository.ts`. Moved it (sync) + the
    `ExternalEffectOutcome`/`ExternalEffectStatus` types into `repository.ts`.
  - Fixed a porcelain-parsing bug: `computeExecutionDelta` reused `parsePorcelain`
    (strips a 3-char `XY ` prefix) on `git diff --name-only` output, which has no
    prefix → `committed.txt` became `mitted.txt`. Now uses plain line-split for the
    diff name-only path.
- Full local suite green: **112 pass, 2 skip** (`test/*.test.mjs`). The 2 skipped are
  `harness-runtime.test.mjs` integration tests 1 & 2, guarded behind
  `MING_HARNESS_TEST=1` (CI-owned: need reviewed bundle + network `pnpm install`).
- Committed as `04ce881`, pushed to `agent/desktop-p0-reconciliation`, opened **Draft PR #22**
  (base `main`) for exact-head CI. Closes ChatGPT audit P0 defects #1/#5/#6/#7/#8/#9/#10.

## CRITICAL env quirk — git branch refs do NOT persist across Bash commands
In this sandbox, `git` commit **objects**, the **index**, and the **working tree** persist,
but `refs/heads/*` and `packed-refs` writes are discarded between commands (even with
`dangerouslyDisableSandbox: true`). Symptom: branch shows "no commits yet" / unborn even
after a commit that printed a SHA. `04ce881` survived as a loose object; the branch ref did not.
**Workaround that works:** chain commits with `git commit-tree -p <parentSha> -m "..."`
(produces a commit object with a real parent), then push by SHA:
`git push origin <newSha>:refs/heads/agent/desktop-p0-reconciliation`. Never rely on a
local branch ref surviving between turns — track the current HEAD SHA yourself.

## Next frontiers (per charter autonomous loop)
- P0-B: prove one bounded real mutation on an ephemeral scratch Git repo through the full
  Intake→Authorize→Execute→Evidence chain. Real Harness run is CI-owned; locally prove the
  chain with a harness-run test double + real git deltas.
- P0-C: technical write boundary / sandbox (prefer Harness sandbox; if Windows can't enforce,
  mark scoped blocker, keep execution disabled in normal UI).
- P0-D: desktop/package verification (Electron dev + packaged + portable launch, zero residual
  processes, resume, no plaintext secret) + merge PR #22 after CI.

## P0-B — DONE (this session)
- Added DI seam `dependencies.runHarnessAcpGrant` to `runBoundedExecution` so the full
  Intake→Authorize→Execute→Evidence chain runs without the reviewed bundle + network (CI-owned).
- New `test/bounded-execution-chain.test.mjs`: on an ephemeral scratch Git repo, a real
  in-scope mutation yields `success` + repository/test evidence; an out-of-scope mutation yields
  `failure` with zero execution-produced changes. Both pass `intendedFiles` (frontier check).
- Suite: **116 pass / 2 skip**. Committed `1e6517e`, pushed, part of PR #22.

## P0-C — DONE (this session)
- `validateExecutionPreconditions` now throws when `mutation_boundary === 'write-authorized'`
  and `allowWrite !== true`. `/api/execute` passes
  `allowWrite: process.env.MING_WORKBENCH_ALLOW_WRITE === '1'`. Reviewed Harness OS sandbox
  preferred; gate is the safety rail where a write sandbox can't be enforced.
- New chain test proves the gate blocks by default and allows when explicitly enabled.
  P0-B tests carry `allowWrite: true` (operator-enabled path).
- Suite: **117 tests / 115 pass / 2 skip**. Committed `56db9a1`, pushed.

## P0-D — PARTIAL / environment-blocked (this session)
- `npm run check` (full tsc --noEmit) passes; `npm run build:test` clean.
- Packaged build (`electron-builder --win dir`) **blocked locally**: electron-builder always
  `emptyDir`s its appOutDir, and the sandbox safe-delete shim routes every delete through
  `genie-trash.exe`, which **times out (ETIMEDOUT)** in this environment. Config is valid
  (parsed, resolved Electron dist=43.4.0, reached copy stage). CI on the pushed commit runs in
  a different runner without this shim, so the packaged build is verified there.
- Remaining P0-D runtime checks (Electron launch, zero residual processes, resume, no plaintext
  secret) need a real desktop session + credentials → charter stop condition (human/environment).
- Added `p0d-build/` + `p0d-build.log` to `.gitignore` (commit `7b7c5f6`) so throwaway build
  artifacts can't be committed. They can't be deleted locally (trash binary times out).
- PR #22 body updated: P0-A/B/C DONE, P0-D pending. Stays Draft until exact-head CI green + P0-D
  runtime checks pass on the human's machine.
